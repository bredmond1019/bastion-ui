name: bastion_ui
description: "BastionUI — Flutter mobile Surface for remotely operating the Bastion practice OS over Tailscale."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # BastionUI core dependencies
  web_socket_channel: ^3.0.3        # WS transport (IOWebSocketChannel for bearer header on handshake)
  flutter_secure_storage: ^9.2.4    # Bearer-token storage (Rule 7 — never shared_preferences)
  flutter_riverpod: ^2.6.1          # State management (fixed by master-plan)
  flutter_markdown: ^0.7.7          # Markdown rendering surface
  flutter_local_notifications: ^19.3.0 # Local notification support (Phase 2+)
  rxdart: ^0.28.0                   # Debounce for high-frequency WS state-refresh streams

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  patrol: ^4.9.0 # Smoke/visual e2e driving a real device/emulator (spike, see planning/decisions/)
  fake_async: ^1.3.1 # Virtual-time control for the rxdart debounce test

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # App launcher icon generation (BU.4.A task5) — regenerates android/app/src/main/res/mipmap-*
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  assets:
    - assets/icon/app_icon.png
    - assets/icon/app_icon_foreground.png

  # Brand type trio (BU.10.A task1) — vendored OFL-licensed variable fonts, NOT the
  # google_fonts package: this app is tailnet-only, so a device off the tailnet must
  # never depend on a runtime font fetch. Each family is a single variable-font TTF;
  # the weight-specific entries below select named instances from it (Flutter resolves
  # the nearest weight axis position from a variable font — see
  # https://docs.flutter.dev/cookbook/design/fonts). Sources (OFL.txt bundled alongside
  # each font under assets/fonts/):
  #   Inter          — https://github.com/google/fonts/tree/main/ofl/inter
  #   Source Sans 3  — https://github.com/google/fonts/tree/main/ofl/sourcesans3
  #   JetBrains Mono — https://github.com/google/fonts/tree/main/ofl/jetbrainsmono
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter.ttf
          weight: 400
        - asset: assets/fonts/Inter.ttf
          weight: 500
        - asset: assets/fonts/Inter.ttf
          weight: 700
    - family: SourceSans3
      fonts:
        - asset: assets/fonts/SourceSans3.ttf
          weight: 400
        - asset: assets/fonts/SourceSans3.ttf
          weight: 500
        - asset: assets/fonts/SourceSans3.ttf
          weight: 700
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono.ttf
          weight: 400

# flutter_launcher_icons config (BU.10.C task7) — run `dart run flutter_launcher_icons`
# to regenerate android/app/src/main/res/mipmap-* from the source assets below. The
# source is the bastiel gem mark: assets/icon/app_icon.png (paper ground + gem, used
# as the legacy/non-adaptive icon) plus an adaptive foreground/background pair —
# assets/icon/app_icon_foreground.png (gem only, transparent) composited over
# adaptive_icon_background (AppTokens.paper's hex, #06070F). ios stays false — this
# is an Android-only Surface (Standing Rule 7 / CLAUDE.md).
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 21
  adaptive_icon_background: "#06070F"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
